#include <bits/stdc++.h>

typedef long long int lint;

#define MOD 1e9+7

using namespace std;

#define pb push_back

int main(){

  int n, m;
  scanf("%d %d", &n, &m);
  while(m--){
    int xi, yi;
    scanf("%d %d", &xi, &yi);
  }

  return 0;
}